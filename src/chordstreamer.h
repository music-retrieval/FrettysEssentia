#ifndef CHORDSTREAMER_H
#define CHORDSTREAMER_H

#include <vector>
#include <string>

namespace chordstreamer {

    std::vector<std::string> compute(const char* audioFilename);

} // namespace chordstreamer

#endif // CHORDSTREAMER_H
